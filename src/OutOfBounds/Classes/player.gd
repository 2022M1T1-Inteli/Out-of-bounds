# Classe do Player
var name: String
var scene: String
var position

# Class Constructor
func _init(_name = "", _scene = "", _position = null):
	self.name = _name
	self.scene = _scene
	self.position = _position

class_name Player
